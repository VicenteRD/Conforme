<%= content_for :heading_main do %>
    <div class="margin-left-50">
      <h4 class="text-capitalize">
        <%= Swot.get_all_types[@swot.swot_type] %>
      </h4>
    </div>
<% end %>

<%# Populate all 3 columns of the view with keys & their values %>

<%= content_for :main_col_1 do %>
    <div class="row">
      <div class="col-lg-4 show-label">Nombre:</div>
      <div class="col-lg-8">
        <%= @swot.name %>
      </div>
    </div>
<% end %>

<%= content_for :main_col_2 do %>
    <div class="row">
      <div class="col-lg-4 show-label">Estrategia:</div>
      <div class="col-lg-8">
        <%= @swot.strategy %>
      </div>
    </div>
<% end %>

<%= content_for :main_col_3 do %>
    <div class="row">
      <div class="col-lg-4 show-label">Responsable:</div>
      <div class="col-lg-8">
        <%= print_responsible @swot.responsible_id %>
      </div>
    </div>
<% end %>



<%= content_for :extra do %>
    <% if @swot.comments? %>
        <div class="panel">
          <div class="panel-heading">
            <h5>Descripción</h5>
          </div>
          <div class="margin-left-30 margin-bottom-30">
            <%= raw @swot.comments %>
          </div>
        </div>
    <% end %>

    <% if @swot.references? %>
        <div class="panel">
          <div class="margin-left-30 margin-bottom-30">
            <%= render partial: 'referables/show_all_for', locals: {element: @swot} %>
          </div>
        </div>
    <% end %>

    <% if @swot.revisions? %>
        <div class="panel">
          <div class="panel-heading">
            <h5>Revisiones</h5>
          </div>
          <div class="panel-body">
            <div class="row margin-left-20 margin-right-20">
              <div class="table-responsive margin-bottom-10" style="height:300px; overflow: auto">
                <table class="table">
                  <thead>
                  <tr>
                    <th>Fecha</th>
                  </tr>
                  </thead>
                  <tbody>
                  <% @swot.revisions.order(revised_at: :desc, created_at: :desc).each do |revision| %>
                      <tr class="revision-row" data-id="<%= revision.id %>" style="cursor: pointer">
                        <td>
                          <%= show_formatted_datetime(revision.revised_at) %>
                        </td>
                      </tr>
                  <% end %>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
    <% end %>
<% end %>

<%= content_for :menu do %>
    <div class="dropdown">
      <% history_content = "#{escape_javascript(render partial: 'log/modal', locals: {book: @swot.log_book})}" %>
      <a href="javascript:void(0)" class="dropdown-inline-button" onclick="showHistory('<%= history_content %>')">
        <i class="dropdown-inline-button-icon icmn-history"></i>
        Bitácora
      </a>
    </div>

    <div class="dropdown">
      <% files_table = "#{escape_javascript(render partial: 'uploaded_files/modal', locals: {attachments: @swot.attachment_ids})}" %>
      <a href="javascript:void(0)" class="dropdown-inline-button" onclick="showAttachments('<%= files_table %>')">
        <i class="dropdown-inline-button-icon icmn-folder-open"></i>
        Adjuntos
      </a>
    </div>

    <div class="dropdown">
      <%= link_to new_swot_revision_path(@swot), method: 'get', class: 'dropdown-inline-button' do %>
          <i class="dropdown-inline-button-icon icmn-rulers"></i>
          Revisar
      <% end %>
    </div>


    <div class="dropdown">
      <%= link_to edit_swot_path(@swot), method: 'get', class: 'dropdown-inline-button' do %>
          <i class="dropdown-inline-button-icon icmn-pencil"></i>
          Editar
      <% end %>
    </div>
<% end %>

<% content_for :breadcrumbs do %>
    <div class="left">
      <ul class="list-unstyled breadcrumb breadcrumb-custom">
        <li>
          <%= link_to swot_index_path do %>
              Estrategias: FODA
          <% end %>
        </li>
        <li>
          <span>
            <%= @swot.number %>
          </span>
        </li>
      </ul>
    </div>
<% end %>

<% content_for :scripts do %>
    <!-- SWOT's Show Page Scripts -->
    <script type="text/javascript">
        $('.revision-row').click(function () {
            var link = '/estrategias/FODA/<%= @swot.id %>/revisiones/' + $(this).attr('data-id');

            window.open(link, '_self');
        });
    </script>
<% end %>
