var modalName;
if (<%= @association_key == 'positions'%>) {
    modalName = 'positions-modal';
} else {
    modalName = 'table-modal';
}

$('#' + modalName + '-body')
    .html("<%= escape_javascript(render partial: 'associables/partials/' + @association_key) %>");

$('#' + modalName).modal();